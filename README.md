# Ai-Storage
Add agents manually via a dialog form with name, description, URL, and category fields Search functionality to quickly find agents by name or description Category filtering with badges showing counts for each category (Chatbot, Image Generation, Code Assistant, Writing, etc.)
- name: vercel-ai
  uses: vercel/ai-action@v2.2.0
